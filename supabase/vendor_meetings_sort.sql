-- ============================================================
-- VENDOR_MEETINGS — 랜딩 목록 수동 정렬 순서(팀 공유)
-- 드래그앤드롭으로 정한 업체 카드 순서를 서버에 저장 → 기기·팀원 공통.
-- sort_order 가 있는 업체가 먼저(오름차순), 없으면 최근 세션순으로 뒤에.
-- 여러 번 실행해도 안전(멱등).
-- ============================================================

alter table public.vendor_meetings
  add column if not exists sort_order int;
