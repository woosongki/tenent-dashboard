-- ============================================================
-- LIVING POPUP — 브랜드명 표기 일괄 정정
-- 시드/사이드바 표기가 변경되었지만 이미 저장된 living_popup 행은
-- 이전 표기를 유지하므로, 사이드바 그리드 매칭이 깨지지 않도록 정정한다.
-- 멱등 — 재실행해도 영향 없음(이미 새 표기인 행은 매치되지 않음).
-- ============================================================

-- 2026-06: 수엔지 → 수앤지
update public.living_popup
   set brand = '수앤지', updated_at = now()
 where brand = '수엔지';

-- 2026-06: 쿤리쿤 → 쿤리콘 (정식 표기)
update public.living_popup
   set brand = '쿤리콘', updated_at = now()
 where brand = '쿤리쿤';
