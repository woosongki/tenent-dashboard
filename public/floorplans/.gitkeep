# 전점 도면 이미지 디렉토리

이 폴더에 점포별 도면 이미지를 추가하세요.

## 파일명 규칙

`{storeId}.{확장자}` 형식 — 예: `nc-junggye.png`, `outlet-anyang.jpg`

지원 확장자: `.png`, `.jpg`, `.jpeg`, `.webp`, `.pdf`

## storeId 목록

`/data/stores/stores.json` 의 `id` 필드 참조.
