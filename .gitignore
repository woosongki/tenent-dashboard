# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — .env.example 은 커밋 허용, 실제 값이 담긴 파일은 제외
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env.vercel.tmp
dist/
data/seoul-hotspots.xlsx

# Claude Code — local 전용은 무시 (settings.json / MCP_SETUP.md는 공유)
.claude/worktrees/
.claude/settings.local.json

# 매출 ERP 원본 (사내 데이터 — 절대 커밋 금지)
/scoredata/

# 매출분석 HTML 내보내기 산출물(데이터 스냅샷, 커밋 제외)
매출분석_오프라인.html
